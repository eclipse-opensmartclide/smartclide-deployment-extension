{
  "private": true,
  "engines": {
    "yarn": ">=1.7.0 <2.x.x",
    "node": ">=12.13.0 <= 12.18.3"
  },
  "scripts": {
    "prepare": "lerna run prepare",
    "rebuild:browser": "theia rebuild:browser",
    "rebuild:electron": "theia rebuild:electron",
    "start:browser": "yarn rebuild:browser && yarn --cwd browser-app start",
    "start:electron": "yarn rebuild:electron && yarn --cwd electron-app start",
    "watch": "lerna run --parallel watch",
    "test": "cd smartclide-deployment-widget && yarn test"
  },
  "devDependencies": {
    "lerna": "2.4.0"
  },
  "workspaces": [
    "smartclide-deployment-widget",
    "browser-app"
  ]
}